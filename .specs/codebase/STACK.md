# Stack & Ecosystem

> Evidence-based map of the project's technology stack. Generated by the SDD Explorer.

## Core
- **Language:** Python **3.14+** (`requires-python = ">=3.14"` in `pyproject.toml`; `.python-version` = `3.14`)
- **Project name:** `agnostic-agent` (version `0.1.0`)
- **Build/package manager:** `uv` (presence of `uv.lock`; no `[build-system]` or `[project.scripts]` declared in `pyproject.toml`)
- **Entry point:** `main.py` → `from cli import run_cli` → `run_cli()` (no console-script registered)

## Runtime Dependencies (`pyproject.toml`)
| Package | Constraint | Purpose |
|---------|-----------|---------|
| `anthropic` | `>=0.116.0` | Anthropic (Claude) API client |
| `google-genai` | `>=2.10.0` | Google Gemini / GenAI API client |
| `openai` | `>=2.44.0` | OpenAI API client (also used by compatible providers) |
| `pydantic` | `>=2.13.4` | Data validation / shared message & tool models |
| `python-dotenv` | `>=1.2.2` | Loads `.env` variables (`load_dotenv()` in `providers/base.py`) |
| `rich` | `>=15.0.0` | Terminal UI (panels, markdown, syntax, status spinners) |

## Dev / Test Tooling
- **Test framework:** `pytest>=9.1.1` (declared in `[dependency-groups].dev`)
- **Test runner:** `uv run pytest`
- **No** linters/formatters (ruff/black) or type-checkers (mypy) declared in `pyproject.toml`.

## External Integrations
- LLM providers via unified abstraction: OpenAI, Gemini, Anthropic, OpenRouter, and OpenAI-compatible endpoints (Ollama, Groq, DeepSeek).
- MCP configs present (`.agents/mcp/chrome-devtools.json`, `.agents/mcp/playwrite.json`) but **not referenced** by any Python source (INCONSISTENT — declared but unused).

## Environment
- `.env` (gitignored) holds API keys; `.env.example` documents OpenRouter defaults.
- `AGENT_PROVIDER` / `AGENT_MODEL` env vars override CLI defaults (`cli.py`).
- `checkpoint.json` (gitignored) written to project root on handover.
